extends layout

block content
	h1= title
	p Istanza: #{instance}
	if !user
		a(href="/login" class="btn btn-spotify")
			i.bi.bi-spotify
			|  Login con Spotify
	else
		h2 Ciao, #{user.spotify_id}
		p access_token:
			pre #{user.access_token}
		p refresh_token:
			pre #{user.refresh_token}
		p La tua email è #{user.email}
		a(href="/account" class="btn btn-spotify") Ottieni 